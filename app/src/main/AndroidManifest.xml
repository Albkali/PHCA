<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.masood.phca">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".BMI_Calculate" />
        <activity android:name=".Article" />
        <activity android:name=".CSHCN" />
        <activity android:name=".Pediatrician" />
        <activity android:name=".Vaccination" />
        <activity android:name=".activity_Register1" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_my_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Register"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".MyDrawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Login_form"
            android:theme="@style/AppTheme.Transparent"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>